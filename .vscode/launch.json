{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Package",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/index.js"
      // "envFile": "${workspaceFolder}/dev.env"
    }
  ]
}
